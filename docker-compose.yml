version: '3.8'

services:
  web_app:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis_db

  redis_db:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes